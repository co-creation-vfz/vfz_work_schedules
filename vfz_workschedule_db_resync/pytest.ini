[pytest]
testpaths = tests
# The integration directory itself is on the path, so `import resync` in a test
# resolves the same way it does when the service runs. Shared helpers are found
# via the sys.path append each module does.
pythonpath = . ../shared
